# Архитектура AliensAttack

## 📋 Содержание

- [Обзор архитектуры](#обзор-архитектуры)
- [Архитектурные принципы](#архитектурные-принципы)
- [Слои системы](#слои-системы)
- [Паттерны проектирования](#паттерны-проектирования)
- [Компоненты системы](#компоненты-системы)
- [Взаимодействие компонентов](#взаимодействие-компонентов)

## 🏗️ Обзор архитектуры

AliensAttack построен на принципах **Clean Architecture** с четким разделением ответственности между слоями. Архитектура обеспечивает высокую тестируемость, гибкость и масштабируемость системы.

### 🎯 Цели архитектуры

- **Модульность** - независимые компоненты
- **Тестируемость** - легкое тестирование каждого слоя
- **Расширяемость** - простое добавление новых функций
- **Производительность** - оптимизированные алгоритмы
- **Надежность** - система обработки ошибок

## 🧩 Архитектурные принципы

### Clean Architecture
Система разделена на слои с четкими границами:

```
┌─────────────────────────────────────┐
│           Presentation Layer        │ ← UI, Controllers
├─────────────────────────────────────┤
│           Business Logic Layer      │ ← Game Logic, Rules
├─────────────────────────────────────┤
│           Domain Layer              │ ← Entities, Value Objects
├─────────────────────────────────────┤
│           Infrastructure Layer      │ ← External Dependencies
└─────────────────────────────────────┘
```

### SOLID принципы

#### Single Responsibility Principle (SRP)
Каждый класс имеет одну ответственность:
- `CombatManager` - управление боем
- `VisibilitySystem` - система видимости
- `ActionManager` - управление действиями

#### Open/Closed Principle (OCP)
Система открыта для расширения, закрыта для модификации:
- Новые типы юнитов через наследование
- Новые действия через интерфейсы
- Новые типы местности через композицию

#### Liskov Substitution Principle (LSP)
Подклассы могут заменять базовые классы:
- Все типы юнитов наследуют от `Unit`
- Все действия реализуют `Action` интерфейс
- Все менеджеры боя реализуют `ICombatManager`

#### Interface Segregation Principle (ISP)
Клиенты не зависят от неиспользуемых интерфейсов:
- `ICombatManager` - только боевые операции
- `IVisibilitySystem` - только видимость
- `IActionManager` - только действия

#### Dependency Inversion Principle (DIP)
Зависимости от абстракций, а не от конкретных классов:
- Внедрение зависимостей через конструкторы
- Использование интерфейсов для взаимодействия
- Инверсия контроля через фабрики

## 🏢 Слои системы

### Presentation Layer (UI)
**Ответственность**: Взаимодействие с пользователем

#### Компоненты:
- `GameWindow` - главное окно приложения
- `Interactive3DDemo` - 3D визуализация
- `Combat3DVisualizer` - визуализация боя

#### Особенности:
- Отделен от бизнес-логики
- Использует события для коммуникации
- Не содержит игровой логики

### Business Logic Layer (Game Logic)
**Ответственность**: Игровая логика и правила

#### Компоненты:
- `CombatManager` - управление боем
- `ActionManager` - управление действиями
- `VisibilitySystem` - система видимости
- `MissionManager` - управление миссиями

#### Особенности:
- Содержит всю игровую логику
- Независим от UI и данных
- Легко тестируется

### Domain Layer (Entities)
**Ответственность**: Бизнес-сущности и правила

#### Компоненты:
- `Unit` - базовый класс юнита
- `Position` - позиция на поле
- `Tile` - клетка поля
- `Action` - действие юнита

#### Особенности:
- Содержит бизнес-правила
- Не зависит от внешних слоев
- Чистые объекты без зависимостей

### Infrastructure Layer (Data & External)
**Ответственность**: Внешние зависимости и данные

#### Компоненты:
- `GameConfig` - конфигурация
- `ErrorHandler` - обработка ошибок
- Файлы ресурсов и конфигурации

#### Особенности:
- Обработка внешних зависимостей
- Управление данными
- Система логирования

## 🎨 Паттерны проектирования

### Factory Pattern
Создание объектов через фабрики:

```java
// WeaponFactory.java
public class WeaponFactory {
    public static Weapon createWeapon(WeaponType type) {
        // Создание оружия по типу
    }
}
```

### Strategy Pattern
Различные алгоритмы для разных типов юнитов:

```java
// ICombatManager.java
public interface ICombatManager {
    CombatResult executeCombat(Unit attacker, Unit target);
}
```

### Observer Pattern
Уведомления об изменениях состояния:

```java
// GameEvent.java
public class GameEvent {
    private EventType type;
    private Object data;
}
```

### Command Pattern
Инкапсуляция действий как объектов:

```java
// UnitAction.java
public class UnitAction {
    private ActionType type;
    private Unit unit;
    private Position target;
}
```

### Singleton Pattern
Единственный экземпляр менеджеров:

```java
// GameEngine.java
public class GameEngine {
    private static GameEngine instance;
    
    public static GameEngine getInstance() {
        if (instance == null) {
            instance = new GameEngine();
        }
        return instance;
    }
}
```

## 🔧 Компоненты системы

### Core Components
- **GameEngine** - главный движок игры
- **GameState** - состояние игры
- **GameConfig** - конфигурация системы

### Combat System
- **ICombatManager** - интерфейс боевой системы
- **CombatResult** - результат боевого действия
- **ShootingSystem** - система стрельбы

### Field System
- **ITacticalField** - интерфейс тактического поля
- **Tile** - клетка поля
- **Position** - позиция на поле

### Action System
- **ActionManager** - управление действиями
- **ActionType** - типы действий
- **UnitAction** - действие юнита

### Visualization System
- **Combat3DVisualizer** - 3D визуализация
- **GameWindow** - главное окно
- **Interactive3DDemo** - интерактивная демо

## 🔄 Взаимодействие компонентов

### Поток выполнения
1. **UI** получает пользовательский ввод
2. **ActionManager** обрабатывает действие
3. **CombatManager** выполняет боевую логику
4. **VisibilitySystem** проверяет видимость
5. **GameState** обновляет состояние
6. **Visualizer** обновляет отображение

### События
Система использует события для слабой связанности:

```java
// Пример события
GameEvent event = new GameEvent(EventType.UNIT_MOVED, unit);
GameEngine.getInstance().notifyObservers(event);
```

### Зависимости
- **UI** → **Business Logic** (через интерфейсы)
- **Business Logic** → **Domain** (прямые зависимости)
- **Infrastructure** → **Business Logic** (через интерфейсы)

## 📈 Производительность

### Оптимизации
- **ConcurrentHashMap** для потокобезопасности
- **Кэширование** результатов вычислений
- **Ленивая загрузка** ресурсов
- **Оптимизированные алгоритмы** поиска пути

### Масштабируемость
- Поддержка полей до 64x64 и больше
- Модульная архитектура для расширения
- Потокобезопасные компоненты
- Эффективное управление памятью

## 🧪 Тестирование

### Архитектура тестирования
- **Unit Tests** - тестирование отдельных компонентов
- **Integration Tests** - тестирование взаимодействия
- **System Tests** - тестирование всей системы

### Mocking
Использование моков для изоляции тестов:
- `MockCombatManager` для тестирования UI
- `MockVisibilitySystem` для тестирования логики
- `MockGameState` для тестирования состояний

## 🔮 Расширяемость

### Добавление новых функций
1. **Новые юниты** - наследование от `Unit`
2. **Новые действия** - реализация `Action` интерфейса
3. **Новые типы местности** - расширение `Tile`
4. **Новые боевые системы** - реализация `ICombatManager`

### Плагинная архитектура
Система поддерживает плагины для:
- Новых типов оружия
- Новых способностей
- Новых типов миссий
- Новых визуальных эффектов
